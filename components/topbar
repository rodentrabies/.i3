general {
        output_format = "i3bar"
        colors = true
        interval = 1
}

order += "tztime local"
order += "slock"

tztime local {
      format = "DATE %Y-%m-%d  TIME %H:%M:%S"
}

slock {
      format = "[L]"
      on_click 1 = "exec slock"
}
